# OHAI Logo Asset

Place the final OHAI logo at:

`public/assets/ohai/ohai-logo.png`

The UI already points to that path through `src/ui/components/OHAILogo.tsx`. Until the binary PNG is added, the component renders a cyan/blue/violet CSS fallback mark inspired by the fingerprint/neural-network logo.
