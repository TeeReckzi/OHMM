# Registry Blockers

> Auto-detected issues blocking registry migration.
> Generated by `npm run validate:data` and `npm run validate:images`.

---

## Critical Blockers (Errors)

### 1. Wikily/OnceHumanWiki URLs (10 occurrences)

Remote URLs to wikily.gg or oncehuman.wiki domains found in source files. These violate the data authority model — presentation-only external data must not be hardcoded in canonical sources.

**Files affected:**
- `src/schemas/externalReferenceSchema.ts:20`
- `src/ui/data/catalog.ts:41, 43, 45`
- `src/ui/registries/generated/weapons.generated.ts:24, 44`
- `src/ui/registries/weaponRegistry.ts:192, 212, 231, 250`

**Resolution:** Remove all wikily URLs. Replace with local image resolution pipeline or remove iconUrl fields entirely.

---

## Warnings (Non-blocking but should be addressed)

### 2. `as any` Casts in Critical Directories (78 occurrences)

Type-unsafe `as any` casts found in engine, resolvers, registries, or presentation code. These bypass TypeScript safety and may hide data model violations.

**Top files:**
- `src/engine/formulaBridgeSmokeTest.ts` (multiple)
- `src/presentation/armorPresentationBridge.ts:26`
- `src/presentation/modPresentationBridge.ts` (multiple)
- `src/ui/registries/` (many registry files)

**Resolution:** Replace with proper type definitions or type guards. Some may be acceptable in test files.

---

### 3. Registry Files Outside `src/registries/` (3 occurrences)

Files with "Registry" in their name located outside the canonical `src/registries/` directory.

**Files:**
- `src/engine/mechanicRegistry.ts` — combat mechanic registry (engine-internal)
- `src/engine/modifierRegistry.ts` — modifier source registry (engine-internal)
- `src/engine/procRegistry.ts` — proc source registry (engine-internal)

**Resolution:** These are engine-internal registries, not canonical item registries. Consider renaming to avoid confusion (e.g., `mechanicBehaviors.ts`, `modifierSources.ts`, `procSources.ts`).

---

### 4. Core-Only Mod Selection Patterns (2 occurrences)

Code patterns that suggest core mod selection without suffix selection.

**Files:**
- `src/presentation/ohdb/mod_future_types.ts:18` — type definition with `coreModId` but no `suffixId`
- `src/ui/registries/modSuffixLegality.ts:8` — function signature with `coreModId` but no `suffixId`

**Resolution:** Update type definitions to require both `coreModId` and `suffixId` for equipped mods. Separate "draft" state from "equipped" state.

---

### 5. Remote URLs in Registry/Data Files (230 occurrences)

HTTP/HTTPS URLs found in registry or data files. Most are in generated files or presentation bridges.

**Top files:**
- `src/ui/data/catalog.ts` (food item icons)
- `src/ui/registries/generated/*.ts` (weapon icons)
- `src/presentation/` (image resolution)

**Resolution:** Review each case. Generated files should be regenerated without remote URLs. Presentation files may need remote fallbacks but should prefer local images.

---

### 6. Empty `statModifiers` in Canonical Registries (45 occurrences)

Canonical registry entries with empty `statModifiers: []` arrays. These represent incomplete data — the mod/armor/weapon has no structured stat effects.

**Top files:**
- `src/ui/registries/armorRegistry.ts` (multiple armor pieces)
- `src/ui/registries/modRegistry.ts` (multiple mods)
- `src/ui/registries/weaponRegistry.ts` (multiple weapons)

**Resolution:** Reconcile with verified data sources. If effects are truly unknown, mark as `confidence: "blocked"` with `blockedReason`. If effects exist in verified data, merge them in.

---

## Image Validation Warnings

### 7. Remote Image URLs (106 occurrences)

Direct references to remote image URLs (http/https with image extensions).

**Resolution:** Same as #5. Prefer local images from `public/assets/ohdb_import_corpus_v2/images_cutout_safe/`.

---

### 8. Duplicate Slashes in Paths (6 occurrences)

Path strings with duplicate forward slashes (e.g., `//`).

**Files:**
- `src/engine/observationParser.ts:27`
- `src/engine/observationSmokeTest.ts:56, 211`

**Resolution:** Fix path construction to avoid duplicate slashes.

---

### 9. Missing Image Extensions (3 occurrences)

Image paths that may be missing file extensions.

**Files:**
- `src/resolvers/armorSetBonusResolver.ts:69, 87, 132`

**Resolution:** These are likely intel link paths (not image paths). Review and add `.md` extension if they're documentation links.

---

### 10. Backslash in Web Path (1 occurrence)

Windows-style backslash in a web path.

**File:**
- `src/ui/components/BuildComparisonPanel.tsx:36`

**Resolution:** Replace backslash with forward slash.

---

## Summary

| Category | Count | Severity |
|----------|-------|----------|
| Wikily URLs | 10 | Error |
| `as any` casts | 78 | Warning |
| Registry location | 3 | Warning |
| Core-only mod | 2 | Warning |
| Remote URLs | 230 | Warning |
| Empty statModifiers | 45 | Warning |
| Remote image URLs | 106 | Warning |
| Duplicate slashes | 6 | Warning |
| Missing extensions | 3 | Warning |
| Backslash paths | 1 | Warning |

**Total: 10 errors, 474 warnings**

---

## Migration Blocker Status

**Registry migration is BLOCKED** until:
1. All wikily URLs are removed (10 errors)
2. Empty statModifiers are reconciled with verified data (45 warnings)
3. Core-only mod patterns are fixed (2 warnings)

**Registry migration is NOT blocked by:**
- `as any` casts (can be addressed incrementally)
- Remote URLs in generated files (can be regenerated)
- Registry files in engine (these are engine-internal, not canonical)
- Image path issues (presentation layer, not registry layer)

---

## Recommended Action Plan

1. **Immediate:** Remove wikily URLs from `weaponRegistry.ts` and `catalog.ts`
2. **Short-term:** Reconcile empty statModifiers with verified data
3. **Medium-term:** Fix core-only mod type definitions
4. **Long-term:** Address `as any` casts and remote URLs

After steps 1-3, registry migration can proceed safely.
